<%= form_for(@typ) do |f| %>
  <% if @typ.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@typ.errors.count, "error") %> prohibited this typ from being saved:</h2>

      <ul>
      <% @typ.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :bezeichnung %><br />
    <%= f.text_field :bezeichnung %>
  </div>
  <div class="field">
    <%= f.label :link %><br />
    <%= f.text_field :link %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
